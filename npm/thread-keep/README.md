# thread-keep

Platform-native binaries for [Thread Keep](https://github.com/tae2089/thread-keep).

```bash
npm install --global thread-keep
thread-keep --help
```

The package installs the matching native binary through npm optional dependencies. It does not run an install script or download an executable from another host.
