# Context Draft Prompt

Use `$thread-keep-context-draft` for the changed Go entities. Inspect the current Thread Keep context and the supplied diff/tests first. Add only evidence-backed pending notes with `--origin agent`, then show `thread-keep --json diff`. Do not change source files, install hooks, call a model API, or run `thread-keep commit`.
